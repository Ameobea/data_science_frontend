//! Routines for exporting data from the database in a variety of formats

/**
 * Given a handle to the PouchDB, retrieves all stored data and dumps it into a JSON file that can be re-loaded at a later point.
 */
function exportJSON(db: any): string {
  // TODO;
  return '';
}

export default {
  exportJSON: exportJSON,
};
